# Testing Standards

- Test every new feature and API endpoint.
- Test both valid inputs (success paths) and invalid inputs (error paths).
- Verify structured error responses from API endpoints.
- Check edge cases (e.g., resource not found, empty database state, invalid input parameters) before completing work.
- Ensure all automated unit tests (both pytest and React testing library) pass before committing.